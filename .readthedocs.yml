version: 2  # Required
formats: all  # Include pdf & epub
python:
  version: 3.7
  install:
    - method: pip
      path: .  # Install from current directory
      extra_requirements:
        - docs
    - method: setuptools
      path: .