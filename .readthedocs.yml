version: 2  # Required
formats: all  # Include pdf & epub
python:
  version: 3.7
  install:
    - method: pip
      path: .
    - method: pip
      path: .  
      extra_requirements:
        - docs
